<!-- GENERATED by spec/specgen from spec/ kvx — DO NOT EDIT. Edit the .kvx source and regenerate. -->

# Implementation Plan: Foundation → Epistemic Core → Security → Memory → Execution → Multiplicity → Liveness → Ecosystem → Presence → Native Workflows → Causal Continuity → Disciplined Work → Embedded Professional Software Studio → Semantic Agent Computer → Agent-Scoped Scheduling → Private Agent Computer and Railway Deployment → Media Studio → Deployment Authentication → Office Studio

## Tasks

## Phase 1 — Foundation (Weeks 1-4)

- [ ] 1. Session store, Vault, O1 Protocol, Tool Registry, Basic Agent Loop
  - [x] 1.1 Project scaffolding + SQLite session store
    - Initialize Go module, CI pipeline, linting config, Makefile
    - Implement SQLite session store with WAL mode, FTS5, schema migrations
    - Implement per-row encryption for messages.content
    - Implement compression-triggered session splitting
    - Test: round-trip encrypt/decrypt, FTS5 search, session splitting, concurrent reads
    - _Requirements: 1.1, 1.2, 1.3, 1.4, 1.5_
  - [x] 1.2 Vault encryption layer
    - Implement AES-256-GCM envelope encryption (KEK → User Key → DEK)
    - Implement KEK source (machine-bound systemd credential for headless Linux; legacy libsecret compatibility)
    - Implement key rotation (atomic, no partial states)
    - Implement key zeroization on shutdown
    - Test: round-trip, wrong key, key rotation, zeroization
    - _Requirements: 2.1, 2.2, 2.3, 2.4, 2.5_
  - [x] 1.3 O1 Protocol + Provider Abstraction
    - Implement NormalizedGeneration, NormalizedToolCall, TokenUsage types
    - Implement ProviderAdapter interface
    - Implement OpenAI adapter (first provider)
    - Implement Anthropic adapter (second provider)
    - Implement provider pool with credential rotation and fallback chain
    - Test: adapter correctness, credential rotation, fallback chain
    - _Requirements: 3.1, 3.2, 3.3, 3.4_
  - [x] 1.4 Self-registering tool registry + basic agent loop
    - Implement registry.register() with check_fn readiness probe
    - Implement tool discovery (scan for register calls)
    - Implement basic agent loop (perception → action)
    - Implement tool execution with timeout and error handling
    - Test: tool registration, readiness probe, basic tool call
    - _Requirements: 4.1, 4.2, 4.3, 4.4_
  - [x] 1.5 Policy pipeline + SSRF dispatcher + security foundation
    - Implement 5-layer policy pipeline (Sandbox, Profile, Provider, Sender, Group)
    - Implement GREEN/YELLOW/RED safety classification enforcement
    - Implement SSRF dispatcher (private IP blocklist, DNS rebinding prevention)
    - Implement KEK source (OS keychain)
    - DAY ZERO GATE: All 30 DZ items must pass
    - Test: all five layers, safety classification, SSRF blocking
    - _Requirements: 5.1, 5.2, 5.3, 5.4, 5.5_

## Phase 2 — Epistemic Core (Weeks 5-8)

- [ ] 2. Premise Ledger, Prediction Dispatch, Self-Model, Cassandra, MMR/SMT
  - [x] 2.1 MMR + SMT + Journal infrastructure
    - Implement BLAKE3 integration
    - Implement MMR append and root computation
    - Implement SMT per-namespace state commitments
    - Implement byte-deterministic replay verification
    - Test: MMR append, root computation, proof generation, proof verification, replay
    - _Requirements: 10.1, 10.2, 10.3, 10.4, 10.5_
  - [x] 2.2 Premise ledger + prediction-carrying dispatch
    - Implement premise extraction from tool calls
    - Implement MMR-bound citations (Citation struct)
    - Implement prediction-carrying dispatch (expect parameter)
    - Implement evidence-delta convergence detection
    - Test: premise extraction, citation verification, prediction mismatch, convergence
    - _Requirements: 6.1, 6.2, 6.3, 6.4, 6.5, 7.1, 7.2, 7.3, 7.4, 7.5_
  - [x] 2.3 Self-model + Cassandra doubt controller
    - Implement self-model (structural, code-derived, evolutionary)
    - Implement immutable safety constraints
    - Implement Cassandra doubt controller (rate-limited)
    - Implement dual-record audit trail
    - Test: self-model evolution, immutable core, Cassandra rate limits, dual-record
    - _Requirements: 8.1, 8.2, 8.3, 8.4, 8.5, 9.1, 9.2, 9.3, 9.4, 9.5, 9.6_
  - [x] 2.4 Phase 2.5: Security integration
    - Implement safety classification enforcement (44 items)
    - Implement emotional state decoupling (CanInfluenceSafety returns false)
    - Implement circuit breakers (frustration, fatigue, cross-axis)
    - Implement sub-agent HMAC authentication
    - Implement closed-verb protocol
    - Implement honeypot memory canaries
    - Implement safety dashboard backend (WebSocket API)
    - GATE: Red-team adversarial test suite passes
    - Test: all SADR decisions, all circuit breakers, all adversarial paths
    - _Requirements: 5.1-5.5, 18.3, 18.4, 16.3, 10.4_

## Phase 3 — Memory (Weeks 9-12)

- [ ] 3. Cortex, HNSW, Activation Composer, Prefetch, Cross-Session Search
  - [x] 3.1 Cortex core: 9-type taxonomy, journal, version chains
    - Implement 9-type taxonomy (Go constants + type checks)
    - Implement append-only journal writer goroutine
    - Implement memory version chains
    - Implement source trust level tracking
    - Test: CRUD with correct types, journal append-only, version chains
    - _Requirements: 10.1, 10.2, 10.3_
  - [x] 3.2 HNSW microservice (Rust) + Go client
    - Implement Rust HNSW service (usearch) over UDS
    - Implement binary protocol for vector insert/search/delete
    - Implement Go client for HNSW service
    - Implement embedding generation
    - Implement fallback to brute-force on service crash
    - Test: insert 10K vectors, search k=10, crash fallback, rebuild on divergence
    - _Requirements: 12.1, 12.2, 12.3, 12.4, 12.5_
  - [x] 3.3 Activation composer + memory prefetch
    - Implement 4+1 tier working set assembly
    - Implement budget trimming by salience
    - Implement memory prefetch pipeline
    - Implement cross-session FTS5 search
    - Test: tier assembly, budget trimming, prefetch <30ms p99, FTS5 search
    - _Requirements: 11.1, 11.2, 11.3, 11.4, 11.5_

## Phase 4 — Execution (Weeks 13-16)

- [ ] 4. Unified Policy-Bound Execution, Honest Partials, Error Recovery, Trajectory Export
  - [x] 4.1 Unified consequential-operation policy, approval, idempotency, and evidence
    - Remove the private rigorous-operation registry, core_execute gateway, rail fields, and misleading placeholders
    - Route consequential operations through the shared production tool manager with accurate RED classification and explicit approval
    - Require idempotency and outcome reconciliation for state-changing operations; retain receipts as optional audit evidence
    - Test: no core_execute surface, unapproved RED denial, approved normal-path execution exactly once, operator-visible evidence
    - _Requirements: 13.1, 13.2, 13.3, 13.4, 13.5_
  - [x] 4.2 Honest partials + error recovery cascade
    - Implement ErrIncomplete
    - Implement 6-layer error recovery cascade
    - Implement tool loop detection + idle timeout breaker
    - Test: stalled turn → ErrIncomplete, cascade layers, loop detection
    - _Requirements: 14.1, 14.2, 14.3, 14.4, 15.1, 15.2, 15.3_

## Phase 5 — Multiplicity (Weeks 17-20)

- [ ] 5. Sub-Agent Spawning, Closed-Verb Protocol, Orphan Recovery, Lanes
  - [x] 5.1 Sub-agent core: spawning, reduced self-model, lanes
    - Implement sub-agent spawning (depth ≤2)
    - Implement ReducedSelfModel (no vault keys)
    - Implement goroutine per sub-agent
    - Implement lane-based concurrency (global, session, agent)
    - Test: spawn with inherited model, lane limits enforced
    - _Requirements: 16.1, 16.5_
  - [x] 5.2 Closed-verb protocol + orphan recovery
    - Implement 10 closed verbs with HMAC authentication
    - Implement parent registry and verification
    - Implement push-based completion
    - Implement orphan detection and recovery
    - Test: HMAC verification, push completion, orphan detection and recovery
    - _Requirements: 16.2, 16.3, 16.4, 16.6_

## Phase 6 — Liveness (Weeks 21-28)

- [ ] 6. Emotional State, Curiosity, Dreamweaver, Forgetting, Social, Goals, Temporal
  - [x] 6.1 Emotional state + metacognitive confidence monitor
    - Implement 6-axis emotional state with decay
    - Implement behavioral modulation
    - Implement metacognitive confidence monitor (decoupled)
    - Implement emotional state persistence
    - Implement circuit breakers
    - Test: decay math, behavioral modulation, safety decoupling, circuit breakers, persistence
    - _Requirements: 18.1, 18.2, 18.3, 18.4, 18.5, 18.6_
  - [x] 6.2 Curiosity engine
    - Implement fringe detection (connection-density percentile)
    - Implement contradiction detection (pairwise consistency)
    - Implement gap detection (recurring unanswerable questions)
    - Implement Automatrix integration
    - Test: curiosity targets from graph anomalies
    - _Requirements: 19.1, 19.2, 19.3, 19.4_
  - [x] 6.3 Dreamweaver
    - Implement memory cluster selection
    - Implement cross-domain pattern matching
    - Implement insight generation (Belief memories)
    - Implement memory consolidation (merge weak memories)
    - Implement provenance tracking and auto-quarantine
    - Test: cycle produces valid Beliefs, Identity/Preference/Constraint untouched, adversarial quarantine
    - _Requirements: 20.1, 20.2, 20.3, 20.4, 20.5_
  - [x] 6.4 Strategic forgetting + social awareness + goals + temporal embodiment
    - Production-compose scheduled memory decay by type (half-lives) with encrypted restart state
    - Rebuild load-bearing premise protection before every archival cycle
    - Production-compose encrypted relationship state, restart-safe trust accounting, authoritative preferences, provider guidance, events, projections, and isolation
    - Production-compose proposal-only goal generation with restart-safe deduplication
    - Production-compose encrypted absolute temporal markers, emotional transitions, provider guidance, events, projections, and restart continuity
    - Implement repair-after-failure identity
    - Implement aesthetic judgment (preferences model)
    - Test: decay rates, load-bearing protection, trust rate limiting, goal generation, temporal modulation
    - Production-compose encrypted first-contact preferred-name capture, provider-context naming, reasoning-event enforcement, restart continuity, and actor isolation
    - _Requirements: 21.1-21.5, 22.1-22.8, 23.1-23.3, 24.1-24.3_

## Phase 7 — Ecosystem (Weeks 29-32)

- [ ] 7. Plugin SDK, Skill System, MCP, Multi-Channel Gateway
  - [x] 7.1 Plugin SDK + skill system + MCP
    - Implement plugin SDK (700+ API surface)
    - Implement skill system (SKILL.md + bundles + Skill Forge)
    - Implement MCP server + client
    - Production-compose first-class Telegram through the shared turn path; retain isolated Discord and Slack gateway contracts
    - Test: plugin registration, skill save/load, MCP server/client, multi-channel
    - _Requirements: 29.1-29.4, 30.1-30.4_
  - [x] 7.2 Operator skill library ingestion and validation-gated self-authoring
    - Recursively discover and normalize every operator-provided SKILL.md bundle while preserving bounded provenance, references, templates, scripts, workflows, and explicit compatibility
    - Install imported bundles durably and idempotently without executing source content or overwriting authored revisions
    - Rank and load all task-applicable skills using trigger, semantic task terms, platform, and ready-tool constraints through a bounded non-authoritative context pack
    - Detect verified novel-task completion and generate a secret-free evidence-backed new-skill candidate while rejecting failed, incomplete, or unverified outcomes
    - Automatically run at least three held-out applicability and safety cases, atomically activate passing candidates, reject unsafe or non-improving candidates, and preserve version and rollback history
    - Expose imported, active, proposed, adopted, and rejected skill state and prove complete-library import, recurrence, refinement, restart durability, capability filtering, and automatic gated activation
    - _Requirements: 30.1-30.9_

## Phase 8 — Presence (Weeks 33-36)

- [ ] 8. Heartbeat, Morning Brief, Automatrix, SOUL.md, Safety Dashboard
  - [x] 8.1 Heartbeat + morning brief + Automatrix + SOUL.md + dashboard
    - Production-compose the 60-second heartbeat, encrypted schedules, missed-run recovery, and truthful projections
    - Implement morning brief (profile-driven, policy-checked)
    - Implement Automatrix (idle-time work queue)
    - Production-compose secure SOUL bootstrap, per-turn loading, proposal/diff/RED approval, atomic history, restart recovery, projections, and rollback
    - Implement safety dashboard frontend
    - Implement weekly integrity digest
    - GATE: Weekly integrity digest operational
    - Test: heartbeat tick, morning brief, Automatrix, dashboard, integrity digest
    - _Requirements: 25.1-25.3, 26.1-26.3, 27.1-27.4, 28.1-28.3_

## Phase 9 — Operator Experience (Weeks 37-46)

- [ ] 9. Shared Control Plane, Full Browser Application, and Terminal UI
  - [x] 9.1 Generated protocol + control plane + secure transports
    - Define the versioned Go command, query, response, event, error, snapshot, and server command-catalog schemas
    - Deterministically generate checked-in TypeScript types and runtime validators as the only client wire contract
    - Implement idempotent optimistic command/query dispatch through production application-service, policy, approval, and audit boundaries
    - Implement bounded durable sequenced event journal, replay, acknowledgements, snapshot fallback, explicit gap markers, and state reconstruction
    - Implement authenticated HTTPS/WebSocket browser transport with CSRF, origin, one-time tickets, rate limits, and payload limits
    - Implement permission-restricted UDS/stdio local transport with short-lived capability authentication and bounded clients
    - Test: headless encrypted session, streamed turn, approval response, every-boundary reconnect, idempotency, optimistic conflict, exact replay, and retention gap
    - _Requirements: 31.1-31.7_
  - [x] 9.2 Web shell + persistent chat + critical operator workflows
    - Build the React/TypeScript application shell, design tokens, responsive navigation, Command Center, and persistent mounted chat host
    - Build safe GitHub-flavored Markdown responses, structured chat, sessions, typed prompts, approvals, task and agent panel, safety console, and integrity console
    - Render production tool, premise, prediction, citation, task, sub-agent, policy, receipt, and memory-provenance events
    - Implement queue, steer, cancel, retry, branch, resume, keyboard command palette, reduced motion, and WCAG 2.2 AA baseline
    - Test: real browser tool conversation and RED approval through production policy and encrypted session storage without navigation stream loss
    - _Requirements: 32.1-32.5_
  - [x] 9.3 Full web subsystem control plane
    - Build memory, epistemics, task graph, swarm, liveness, Automatrix, tools, execution, policy, receipt, and integrity surfaces
    - Build provider, MCP, plugin, skill, channel, presence, schedule, configuration, SOUL, logs, and diagnostics surfaces
    - Route every supported mutation through its production application service, policy, approval, revision, and audit boundary
    - Sandbox and permission-scope plugin UI surfaces; implement write-only secret controls and redacted diagnostics
    - Test: primary-subsystem read matrix, mutation boundary matrix, secret redaction, and cross-session/profile/channel isolation
    - _Requirements: 33.1-33.5_
  - [x] 9.4 First-class terminal UI
    - Build the Ink/React shell on the shared generated protocol, client, reducer, formatter, and server command catalog
    - Build chat, composer, queues, prompts, approvals, sessions, providers, tasks, projects, schedules, skills, agents, inspectable memory, Computer, security, extensions, logs, and diagnostics workflows
    - Implement attach, supervised-local, and optional remote TLS modes with bounded startup, RPC, buffering, reconnect, retry, and shutdown
    - Implement keyboard-only operation, terminal color fallbacks, width-aware Unicode, portability profiles, and complete terminal-state restoration
    - Test: browser-parity journey, deterministic reconnect and crash recovery, command parity, terminal interactions, and terminal restoration matrix
    - _Requirements: 34.1-34.5_
  - [x] 9.5 Frontend and TUI hardening, packaging, and release integration
    - Embed the deterministic production web bundle and add `ion dashboard`
    - Bundle the deterministic TUI artifact and add `ion tui` attach and supervised modes
    - Add browser and terminal matrices plus restart, replay, backpressure, corruption, malformed-frame, authorization, isolation, and event-flood suites
    - Enforce JavaScript, startup, render-latency, memory, accessibility, and slow-client performance budgets
    - Integrate clean-install release packaging and generate operator documentation from implemented catalogs
    - Test: clean install runs both clients, survives daemon restart, and passes security, accessibility, portability, and performance gates
    - _Requirements: 35.1-35.6_

## Phase 10 — Native Agent Workflows

- [ ] 10. Browser Runtime, Dedicated Mailbox, and Agent-Native Identity
  - [x] 10.1 Native browser and private mailbox verification vertical slice
    - Replace the MCP-dependent browser placeholder with actor/session-isolated native Chromium/CDP navigation, observation, reversible interaction, and RED submit tools
    - Intercept browser requests, reject unsafe destinations, bound semantic page projections, and keep browser profiles volatile and private
    - Implement a receive-only IMAP/TLS source plus Vault-encrypted verification store with redacted metadata
    - Transfer codes and confirmation links server-side into an origin-matched browser target through a RED approval without returning the secret
    - Test: real Chromium workflow, sensitive-field refusal, unapproved RED denial, encrypted mailbox restart, private verification transfer, and exactly-once submit
    - _Requirements: 36.1, 36.2, 36.3, 36.4, 36.6_
  - [x] 10.2 Supervised browser sessions, credential references, and operator handoffs
    - Add browser workflow status, live semantic preview, pause, resume, cancel, and exact approval consequence surfaces to web and TUI
    - Add write-only Vault credential references and origin-bound browser insertion without exposing secret values to the model or browser event stream
    - Add typed human handoffs for CAPTCHA, passkeys, legal identity, terms, payment, recovery, and ambiguous consequential controls
    - Add restart reconciliation for in-progress browser workflows without persisting plaintext cookies, codes, or form secrets
    - Test: adversarial pages, redirect and subresource SSRF, prompt injection, stale refs, popup/download boundaries, cancellation, restart, and secret redaction
    - _Requirements: 36.2, 36.3, 36.4, 36.6_
  - [x] 10.3 Interoperable agent-native signup and delegated identity profile
    - Specify well-known discovery, WebAuthn-compatible agent credential proof, user delegation, requested scopes, expiry, revocation, and recovery
    - Implement relying-party and agent conformance fixtures with origin binding, replay resistance, least authority, and explicit service policy
    - Expose Agent account as a third signup choice without claiming personhood or bypassing age, identity, legal consent, payment, or abuse controls
    - Publish a versioned protocol document and compatibility test vectors
    - Test: origin confusion, replay, revoked delegation, scope escalation, phishing relay, account recovery, and human-account separation fail closed
    - _Requirements: 36.5_

## Phase 11 — Causal Liveness and Visible Continuity

- [ ] 11. Enforced Decision Policy, Production Repair, Idle Action, Return Briefs, and Longitudinal Proof
  - [x] 11.1 Typed causal liveness decision policy
    - Derive bounded planner and execution parameters from emotional, temporal, relationship, confidence, repair, and confirmed-aesthetic state
    - Enforce retry, tool, exploration, and fan-out bounds in the shared production agent loop
    - Persist and project user-safe causal explanations while keeping raw state under technical details
    - Test counterfactual behavior with invariant safety, approval, evidence, and required verification
    - _Requirements: 37.1, 37.2_
  - [x] 11.2 Production repair, aesthetic selection, and idle completion
    - Wire failure, recovery, evidence, lesson, prevention, and future activation through the production turn lifecycle
    - Wire explicit confirmed aesthetic preferences into solution selection with review and revision controls
    - Compose approval-gated Automatrix execution and sub-agent result synthesis through the heartbeat
    - Add causal provenance, cooldowns, and deduplication for event-driven curiosity and Dreamweaver work
    - _Requirements: 37.3, 37.4, 37.5, 37.6_
  - [x] 11.3 Since-you-were-away and typed morning brief experience
    - Build actor-scoped return briefs from allowlisted durable evidence without fabricated activity
    - Surface discoveries, repairs, proposals, approvals, incomplete work, and deadlines in web and TUI
    - Show behavioral consequences by default and keep numeric liveness state under technical details
    - _Requirements: 37.7_
  - [x] 11.4 Reviewable relationship profile and normal-timescale temporal embodiment
    - Implement inspect, correct, pin, and remove controls for explicit and inferred communication and workflow preferences
    - Apply gradual early, middle, stagnant, deadline, interrupted, return, long-session, and closure temporal decisions
    - Propose a richer SOUL v2 through the existing diff, RED approval, history, and rollback path
    - _Requirements: 37.8, 37.9_
  - [x] 11.5 Longitudinal restart and anti-performance acceptance
    - Run multi-day restart, cross-actor, counterfactual, exactly-once, and later-activation production journeys
    - Prove no activity, feeling, insight, repair, preference, or continuity claim exists without durable evidence
    - Reconcile Phase 6, Phase 8, Phase 9, and Phase 11 statuses only after live browser, TUI, restart, and provider acceptance
    - _Requirements: 37.10_

## Phase 12 — Disciplined Autonomous Work

- [ ] 12. Outcome Contracts, Evidence Registry, Review Lenses, Autonomy Controls, and Workflow Recipes
  - [x] 12.1 Durable work brief and evidence-bound completion vertical slice
    - Implement encrypted actor-scoped outcome contracts and derive a concise current Work Brief and next action
    - Implement a workspace-confined artifact registry with server-side hashing, criterion coverage, and verification evidence
    - Implement deterministic risk-based review lenses and curated workflow recipes with entry, exit, evidence, and human-gate metadata
    - Implement off, suggest, and approved autonomy modes plus pause, resume, tool, token, time, error, and cooldown budgets without weakening policy
    - Expose production control-plane operations and task-first web and TUI projections using the shared generated contract and reducer
    - Test encrypted restart, cross-actor isolation, path and symlink attacks, false-completion denial, exact budget enforcement, and workflow resumption
    - _Requirements: 38.1-38.8_

## Phase 13 — Embedded Professional Software Development Capability

- [ ] 13. One Full-Range Agent with Projects, Isolated Workspaces, Spec-Driven Engineering, Live Preview, Verification, Delivery, and Expert Packs
  - [x] 13.1 Project registry and versioned WorkspaceHost contract
    - Implement encrypted actor-scoped project registry for template creation, archive import, existing-directory attachment, and authorized repository clone
    - Define generated WorkspaceHost capabilities and operation envelopes for project, files, processes, Git, preview, artifacts, integrations, and system functions
    - Implement direct-local and rootless container hosts with runtime capability negotiation, resource and egress policy, secret grants, and typed lifecycle events
    - Implement provision, readiness, pause, resume, archive, and teardown reconciliation without losing artifacts or uncommitted work
    - Test local and container create, import, clone, restart, isolation, resource exhaustion, stale revision, and duplicate mutation
    - _Requirements: 39.1-39.6_
  - [x] 13.2 Plain-language intent compiler and authoritative spec workflow
    - Bind software requests to project revisions and outcome contracts and inspect repository instructions before planning
    - Implement proposed, accepted, rejected, and superseded spec deltas with user-visible behavior, constraints, criteria, risks, verification, rollback, and task dependencies
    - Implement assumption and material-ambiguity policy plus criterion correlation for tasks, patches, tools, reviews, evidence, and artifacts
    - Implement spec-to-code and generated-view drift detection that blocks unsupported completion
    - Test greenfield, brownfield, ambiguity, scope change, restart, rejected proposal, drift, and false-completion journeys
    - _Requirements: 40.1-40.6_
  - [x] 13.3 Incremental code graph, search, and context planner
    - Implement ignore-aware project inventory, stack and entry-point discovery, symbol and reference index, dependency graph, and repository-instruction resolver
    - Implement revision-cited lexical, regex, filename, symbol, reference, dependency, diagnostic, and semantic retrieval
    - Implement task-specific progressive context packs with explicit omission, truncation, protected-path, generated, binary, and secret handling
    - Integrate current spec, Work Brief, Git history, diagnostics, skills, and verified evidence into context selection
    - Benchmark incremental freshness, monorepo scoping, multi-language navigation, secret exclusion, stale citations, and retrieval quality
    - _Requirements: 41.1-41.6_
  - [x] 13.4 Atomic patch sets, durable terminals, and dependency policy
    - Complete safe project file operations and implement baseline-bound multi-file patch sets with preflight, hashes, rollback, and crash reconciliation
    - Implement durable supervised processes and PTYs with stream replay, input, resize, signals, cancellation, process-tree cleanup, output bounds, and redaction
    - Implement runtime and package-manager discovery plus lockfile-preserving dependency installation and package-script supply-chain policy
    - Implement optimistic revisions and conflict surfaces for concurrent human and agent edits while preserving every pre-existing dirty change
    - Test atomicity, stale patches, rollback, PTY reconnect, floods, runaway processes, package attacks, symlink swaps, and concurrent edits
    - _Requirements: 42.1-42.6_
  - [x] 13.5 Git versions, reviewable changes, and repository providers
    - Implement read-only Git baseline, status, history, blame, branch, remote, staged and unstaged diff, rename, binary, and generated-file projections
    - Implement separately authorized branch, checkpoint, commit, tag, sync, push, merge, and pull-request operations with exact change scoping
    - Implement criterion-grouped change review, hunk selection, comments, and durable review work items
    - Implement recovery-safe historical preview and transactional code or conversation restore
    - Implement normalized provider adapters for repositories, branches, issues, pull requests, checks, review threads, and mergeability
    - Test dirty-tree preservation, partial staging, conflicts, interrupted preview, rejected remote mutation, pagination, stale reviews, and exactly-once draft PR creation
    - _Requirements: 43.1-43.6_
  - [x] 13.6 Runtime manager, secure live preview, and unified diagnostics
    - Implement inferred and configured install, build, dev, test, and start commands with port allocation, readiness, named services, reload, restart, stop, and crash recovery
    - Implement origin-isolated preview with address and service controls, viewport presets, screenshots, browser inspection, and safe external opening
    - Normalize console, process, build, type, lint, browser, network, and source-map evidence into deduplicated Problems and bounded fix workflows
    - Implement DOM and accessibility inspection, element selection, annotations, and reviewable visual-edit patch proposals
    - Run representative static, frontend, API, and full-stack browser matrices including mobile, dark mode, hostile preview, crash repair, and cleanup
    - _Requirements: 44.1-44.6_
  - [x] 13.7 Verification manifest, repair loop, and independent release review
    - Derive versioned per-project verification manifests from accepted spec criteria and inspected repository commands
    - Implement targeted-to-full gate execution with revision-bound logs, reports, coverage, screenshots, traces, hashes, and criterion coverage
    - Implement stable failure signatures and bounded diagnose, patch, rerun loops with regression, stagnation, flake, environment, and budget stops
    - Extend review routing with independent data, migration, accessibility, performance, operations, and release evidence lenses
    - Implement explicit scoped expiring waivers that remain uncovered rather than passing
    - Test injected compile, test, visual, accessibility, security, migration, performance, flake, timeout, and false-green failures
    - _Requirements: 45.1-45.6_
  - [x] 13.8 Full-stack resources, environments, CI, deployment, and rollback
    - Implement provider-neutral desired-state catalog and adapters for data, storage, authentication, email, queues, schedules, analytics, payments, and external APIs
    - Implement versioned environment schemas and least-authority write-only secret grants across development, test, preview, staging, and production
    - Implement migration inspect, generate, dry-run, isolation, destructive analysis, ordered promotion, backup, and rollback workflows
    - Implement reviewed CI patches plus preview, staging, and RED production deployment plans with immutable artifacts, idempotency, reconciliation, health, cost, and rollback evidence
    - Implement release notes, versioning, domain and DNS readiness, provider conformance, portable export, and one live staging acceptance journey
    - _Requirements: 46.1-46.6_
  - [x] 13.9 Embedded responsive Software Studio and intent-to-preview experience
    - Build task-first project home with prompt-preserving create, import, clone, template, recent, and resume flows
    - Keep conversation and Work Brief mounted while adding adaptive Plan, Changes, Code, Terminal, Preview, Problems, Tests, Security, Data, and Deploy panels
    - Render structured engineering, verification, sub-agent, recovery, and deployment activity from the shared generated reducer with progressive technical detail
    - Implement steering, decisions, spec revision, plan and diff comments, preview selection, version restore, and terminal or browser takeover without races
    - Pass WCAG 2.2 AA, keyboard, screen-reader, reduced-motion, localization, 390px-to-wide-desktop, dark and light, and expert-shortcut acceptance
    - Run first-time-user intent-to-staging and expert diff, terminal, Git, diagnostics, and recovery browser journeys, then return to field-general work without identity or continuity loss
    - Make every visible Studio entry path misuse-tolerant: create or bind structured project scope before mutation, preserve that scope across queue, steer, retry, reconnect, restart, and navigation, and reject contradictory or missing scope without falling back to the general workspace
    - _Requirements: 47.1-47.9_
  - [x] 13.10 Ion engineering supervisor and restart-safe specialist swarm
    - Compile accepted specs into durable dependency DAGs and dispatch only ready work through one authoritative supervisor state
    - Define least-authority task and evidence contracts for discovery, exploration, implementation, test, security, data, frontend, performance, operations, and review specialists
    - Implement file and service scopes, leases, bounded fan-out, deterministic merge order, cancellation propagation, and structured result synthesis
    - Persist and reconcile queue, attempts, patches, processes, verification, approvals, budgets, artifacts, Git, and external effects before retry
    - Enforce per-project and per-outcome token, cost, tool, wall-time, process, storage, network, provider-spend, retry, and parallel-agent budgets
    - Fault-test conflicting edits, child and parent failure, restart, duplicates, uncertain effects, steering, cancellation, and budget exhaustion
    - _Requirements: 48.1-48.6_
  - [ ] 13.11 Issue, review, CI, webhook, schedule, and maintenance intake
    - Normalize authorized engineering intake with untrusted-content boundaries, permissions, project and revision routing, deduplication, and delivery targets
    - Implement spec-gated recipes for issue work, failing checks, review comments, dependencies, vulnerabilities, releases, documentation, flakes, and incidents
    - Run automations in isolated branches or workspaces with bounded concurrency, cooldowns, poison-item handling, and exactly-once delivery
    - Project honest durable status and evidence back to sources without secrets, private reasoning, or unrelated workspace data
    - Test injection, replay, duplicates, stale and force-updated revisions, provider outage and rate limits, review resolution, CI reruns, and draft delivery
    - _Requirements: 49.1-49.6_
  - [ ] 13.12 Expert Pack framework and demonstrated cross-domain capability
    - Define versioned Expert Packs with ontology, tasks, sources, methods, tools, workflows, safety, scope, freshness, schemas, verification, benchmarks, and limits
    - Implement evidence-based pack routing, user inspection and control, source freshness, uncertainty, human-specialist escalation, and self-model integration
    - Implement provenance-preserving candidate generation, private-data redaction, adversarial evaluation, review, activation, rollback, confidence reduction, and quarantine
    - Implement qualified-human and jurisdiction gates for high-stakes medical, legal, financial, safety, security, and regulated workflows
    - Build reproducible correctness, calibration, source, tool, policy, and transfer evaluations across software, research, operations, data, design, writing, and regulated fixtures
    - _Requirements: 50.1-50.7_
  - [ ] 13.13 Clean-install intent-to-staging production acceptance
    - Build TypeScript frontend, full-stack web, Go, Python, Rust or systems, and brownfield monorepo fixtures with stack-appropriate security, accessibility, migration, restart, and rollback cases
    - Run one-request clean-install journeys from accepted spec through implementation, preview, injected-defect repair, verification, change review, and isolated staging deployment
    - Prove browser and TUI switching, reconnect, daemon and host restart, provider fallback, long pause and resume, concurrent edits, version restore, and exact durable reconciliation
    - Run adversarial repository, issue, dependency, secret, filesystem, preview, command, network, event, cross-scope, and unapproved-publication suites
    - Publish provisioning, index, stream, preview, replay, memory, cancellation, orphan-cleanup, and multi-hour SLO evidence on declared hardware
    - Reconcile Phase 13 and dependent umbrella statuses only after live production composition, criterion artifacts, truthful remaining boundaries, and portable project export are proven
    - _Requirements: 51.1-51.6_
  - [x] 13.14 Provider-compatible resilient task execution and evidence convergence
    - Implement first-class MiMo request, response, reasoning, textual-call, and buffered-stream compatibility with agentic sampling while keeping raw reasoning and internal compatibility or revision instructions and outputs outside transcript history and every client stream
    - Run a harmless lazy multi-turn provider capability canary and project the selected compatibility mode
    - Make workspace file and process tools repair missing parents, contained absolute paths, change-directory intent, and unavailable executables safely
    - Persist dependency-aware criterion work items and advance them only from server-verified evidence
    - Require every recovery retry to advance a material provider or execution strategy and scope prediction mismatches to the exact normalized action in the current turn
    - Add a bounded activity watchdog plus exact running, recovering, incomplete, and terminal browser and TUI projections
    - Gate every model final on direct satisfaction of the original request and gate active Studio outcomes additionally on durable evidence convergence
    - Fault-test compatibility, misuse, exact-action mismatch isolation, typed revision continuation, raw-reasoning suppression, false-final repair, restart, watchdog, projection, and premature-completion paths through production composition
    - _Requirements: 52.1-52.8_

## Phase 14 — Ion Computer: Semantic Visualization of Real Agent Work

- [ ] 14. Tool-Triggered Events, Validated Display Models, Native Applications, Replay, Parallel Workspaces, and Anti-Theater Acceptance
  - [x] 14.1 Canonical ComputerEvent protocol and durable tool-lifecycle projection
    - Define versioned generated ComputerEvent schemas for requested, approval, started, bounded progress, completed, failed, denied, interrupted, and outcome-unknown tool states
    - Instrument the shared production tool manager once so real tool calls trigger activity and authoritative tool responses, errors, approvals, artifacts, and evidence populate correlated terminal events
    - Persist actor, session, outcome, task, agent, tool-call, scope, risk, sequence, timestamp, display hint, and source references without duplicating execution or completion truth
    - Implement coalescing, payload bounds, backpressure, acknowledgements, replay, snapshot and gap recovery, duplicate suppression, and deterministic multi-agent ordering
    - Generate Go and TypeScript types, validators, migrations, reducer fixtures, and protocol compatibility tests
    - Fault-test success, approval, denial, streaming, failure, interruption, uncertain outcome, slow client, disconnect, duplicate replay, retention gap, and daemon restart
    - _Requirements: 53.1-53.6_
  - [x] 14.2 Deterministic tool-response adapters and semantic display-model registry
    - Define generated reusable display models for search, reader, navigation, repository, code, terminal, diff, process, table, chart, document, artifact, task, agent, approval, error, and degraded views
    - Implement versioned adapters that prefer structured tool-response fields, attach observed, generated, summarized, inferred, or unavailable classification, and preserve evidence references
    - Implement strict schema validation, text and URL normalization, HTML and media sanitization, terminal-control neutralization, redaction, size bounds, and generic fallback
    - Implement display-model migration and explicit unsupported-version behavior for retained event replay
    - Build adapter conformance fixtures from real bounded tool results without copying secrets or private workspace data into test assets
    - Test exact extraction, malformed and oversized results, missing fields, contradictory summaries, untrusted markup, fallback, migration, and evidence linkage
    - _Requirements: 54.1-54.7_
  - [x] 14.3 Resizable live computer shell, timeline, history, and explicit interaction states
    - Add a resizable persistent-conversation and Ion Computer layout with full-screen, adaptive mobile, active application, agent, action, status, and latest-event header
    - Implement Follow live, pause following, durable timeline navigation, historical inspection, independent user selection, and Back to live on the shared reducer
    - Separate read-only Watch and Explore state from explicit queue, steer, retry, approve, deny, pause, resume, cancel, and takeover commands
    - Implement honest loading, empty, awaiting-decision, recovering, blocked, failed, interrupted, uncertain, completed, retention-gap, and unsupported states without fabricated cursor, typing, or progress
    - Implement keyboard and screen-reader critical paths, reduced motion, localization-safe layout, dark and light themes, bounded history virtualization, and terminal-appropriate parity
    - Run real web and TUI resize, full-screen, follow, history, steer, reconnect, refresh, route, mobile, accessibility, reduced-motion, and long-history journeys
    - _Requirements: 55.1-55.8_
  - [x] 14.4 Ion-native Research, Repository, Code, Terminal, Document, Data, and Task applications
    - Build Research Browser search and reader views from real search and semantic browser results with URLs, source identity, citations, navigation, media references, and screenshot-on-demand evidence
    - Build Repository and Code views from provider, browser, project, Git, search, file, diff, diagnostic, revision, branch, and path results
    - Build Terminal and Process views with bounded redacted streaming output, working directory, duration, exit, service, retry, cancellation, and artifact state
    - Build Document, Artifact, Data, Task, and Agent views with provenance, versions, tables, charts, files, assignments, decisions, budgets, blockers, evidence, and criterion coverage
    - Register renderers through the generated display contract, share generic Ion primitives, and preserve an inspectable generic view for absent or incompatible applications
    - Run the renderer matrix against real search, article, GitHub or GitLab, local workspace, code, Git, terminal, process, diff, report, table, artifact, error, unsupported-tool, and screenshot-fallback paths
    - _Requirements: 56.1-56.8_
  - [x] 14.5 Parallel-agent workspaces, causal aggregation, steering, and takeover leases
    - Project stable parent and sub-agent identities, assignments, applications, actions, status, evidence, budgets, blockers, and artifacts from authoritative task and tool events
    - Implement aggregate and per-agent workspaces with visible foreground inspection, background execution, independent Follow live positions, and causally honest ordering
    - Bind every steering command to an explicit task, agent, tool action, scope, and revision with stale, contradictory, cross-scope, and unauthorized rejection
    - Implement browser and terminal takeover leases that coordinate the owning executor, show authority and expiry, and reconcile control without duplicated effects or hidden automation
    - Test simultaneous tool calls, agent switching, historical inspection, stale steering, approval routing, cancellation, takeover expiry, child failure, parent restart, duplicates, and final synthesis
    - _Requirements: 57.1-57.6_
  - [ ] 14.6 Computer security, privacy, attribution, accessibility, and performance hardening
    - Enforce inert rendering, safe URL and media policy, origin isolation, server-side actor and workspace authorization, and prompt-injection data boundaries
    - Redact secrets and protected content before client delivery across events, display models, screenshots, caches, exports, diagnostics, and share paths
    - Bind approval and consequence surfaces to the real operation, target, data movement, scope, status, and authoritative policy record
    - Publish and enforce event, delta, payload, retention, screenshot, memory, CPU, render, replay, reconnect, and slow-client budgets with terminal evidence preserved
    - Retain source identity, URL, capture time, revision, and truth classification so Ion-native views cannot be mistaken for original external interfaces
    - Run adversarial injection, markup, media, terminal escape, secret, cross-scope, stale authorization, forged event, flood, screenshot, attribution, accessibility, and performance suites
    - _Requirements: 58.1-58.7_
  - [ ] 14.7 Real GitHub-analysis production journey and Phase 14 anti-theater gate
    - Run one real repository-analysis request through search, browsing, repository navigation, code inspection, bounded commands or analysis, optional parallel agents, artifact creation, and report review
    - Correlate every visible application transition, datum, status, failure, artifact, and completion claim to durable production events and criterion evidence
    - Prove refresh, route change, disconnect, reconnect, daemon restart, snapshot and gap recovery, duplicates, out-of-order completion, agent failure, history inspection, and Back to live
    - Run the complete renderer, approval, denial, incomplete, uncertain, unsupported-tool, screenshot, mobile, keyboard, screen-reader, reduced-motion, security, and attribution matrix
    - Publish reference-hardware event-to-projection, interaction, replay, memory, payload, long-run, event-flood, and slow-viewer performance evidence
    - Reconcile Phase 14 status only after production binaries and real clients prove every criterion with zero visual activity lacking an authoritative source event
    - Prove the same Ion identity, memory, relationship, policy, goals, approvals, and execution continuity before, during, and after software, research, data, document, and communication work
    - _Requirements: 59.1-59.7_
  - [x] 14.8 Search-first public research reliability repair
    - Default public research to https://browsingmachine.com/ while preserving explicit ION_SEARCH_ENDPOINT and legacy SEARXNG_URL deployment overrides
    - Return bounded ranked source metadata, automatically try one news-category fallback when default general engines return no results, and report explicit upstream-engine degradation instead of raw HTML or silent empty results
    - Instruct the production agent to search before semantic fetch or native browsing, treat SearXNG fallback as part of one search call, and avoid repeated degraded queries or guessed direct URLs
    - Test default readiness, result normalization, degraded-engine reporting, and production research context
    - _Requirements: 56.9_
  - [x] 14.9 Tavily-first research routing and native Computer presentation
    - Load a bounded Tavily credential and endpoint through the restricted production environment and make Tavily the primary web_search provider
    - Preserve SearXNG as an automatic bounded fallback with explicit provider, category, degradation, and fallback metadata
    - Route ordinary research through structured search and selected semantic fetch while reserving native browser or graphical computer use for direct interaction, authentication, downloads, visual inspection, JavaScript-only pages, or insufficient earlier layers
    - Project the Tavily overview, provider state, ranked URLs, snippets, publication dates, source domains, and relevance into the inert Ion-native Research application
    - Add normalization, routing, environment, display-model, and UI test source and verify static builds without executing tests
    - _Requirements: 56.1, 56.6-56.9, 61.4_

## Phase 15 — Agent-Scoped Scheduling

- [ ] 15. Per-agent Chronos scheduling, durable wake-up, and operator control
  - [x] 15.1 Adapt Chronos into the encrypted per-agent production runtime
    - Adapt once, absolute, cron, descriptor, interval, timezone, owner scope, idempotency, claim, retry, and skip-advance semantics without the central Postgres or router services
    - Persist alarm content in actor-scoped encrypted state and recover due, claimed, failed, cancelled, and recurring alarms across daemon restart
    - Register direct create, list, inspect, and cancel tools and inject due wakes through idempotent authenticated production turns
    - Project agent-created schedules separately from internal presence maintenance and expose truthful status, next fire, last fire, failure, and safe error state
    - Test actor isolation, encryption, calculations, idempotency, claims, retries, cancellation, exact-once wake, recurring advancement, restart recovery, policy continuity, and live clients
    - _Requirements: 60.1-60.7_

## Phase 16 — Private Agent Computer and Railway Deployment

- [ ] 16. Unprivileged Personal and Clean Linux Desktops, Live Takeover, and Portable One-Click Hosting
  - [x] 16.1 Private Computer contracts, lifecycle, and deployment topology
    - Define versioned host, session, capability, lifecycle, authority, persistence-mode, budget, compatibility, and reconciliation contracts
    - Bind every privileged request to installation, actor, Ion session, task or outcome, agent, computer session, operation, resource, authority revision, expiry, idempotency, and replay protection
    - Define legal Personal and Clean lifecycle transitions, explicit degraded and unavailable states, artifact-before-destroy gating, and orphan recovery
    - Define the two-service Ion and ion-computer topology, authenticated private channel, public stream proxy, volumes, secrets, health, restart, rollback, export, and cost boundaries
    - Record the native-tool-to-graphical-fallback hierarchy and canonical ComputerEvent, ToolEvent, lease, policy, artifact, and evidence correlation without a second completion truth
    - Test strict validation, negotiation, transitions, idempotent replay, stale authority, cross-scope isolation, bounded serialization, topology invariants, and restart reconciliation
    - _Requirements: 61.1-61.7_
  - [ ] 16.2 Unprivileged Linux desktop host and Personal/Clean lifecycle
    - Build a version-pinned non-root Linux desktop image with browser, terminal, files, approved applications, read-only base, dropped capabilities, scoped mounts, and explicit capability probes
    - Implement authenticated provision, start, stop, suspend, resume, rebuild, destroy, inspect, and reconcile operations through the versioned host contract
    - Implement actor-scoped Personal volumes and cryptographically fresh Clean workspaces with independent resource, storage, idle, session, and egress budgets
    - Gate Clean destruction on authoritative artifact export and cleanup evidence and implement orphan, partial-cleanup, restart, storage-exhaustion, and unavailable recovery
    - Expose browser, terminal, filesystem, screenshot, pointer, keyboard, clipboard, stream, protected-secret-entry, and approved-application capabilities without a public control port
    - Run real container lifecycle, persistence, freshness, cleanup, isolation, budget, restart, and hostile-download tests without privileged mode
    - _Requirements: 61.1-61.7, 62.1-62.7, 65.1-65.5_
  - [ ] 16.3 Live desktop session transport and Ion Computer renderer
    - Implement separate short-lived view and input tickets plus authenticated bounded desktop transport through the Ion control plane
    - Render a real private_desktop application inside the existing ComputerStage with isolated untrusted pixels and no raw daemon access from the browser
    - Add explicit Computer management route for setup, readiness, mode, applications, sessions, resource use, storage, and recovery while preserving idle embedded-computer visibility
    - Make the active desktop dominant beside persistent conversation and evidence, preserve Follow live, history, full-screen, mobile mounted-state switching, keyboard, screen reader, and reduced motion
    - Implement bounded Railway-compatible reconnect, replay cursors, duplicate suppression, route and refresh survival, and truthful disconnected, recovering, failed, and unavailable states
    - Run real service and browser tests proving authenticated frames, event correlation, reconnect, restart, mobile, accessibility, and zero fabricated activity
    - _Requirements: 63.1-63.2, 63.5, 63.7-63.8_
  - [ ] 16.4 Lease-based takeover, credential boundaries, and restart recovery
    - Route Pause, Take control, Renew, Return control, Stop, and Rebuild through production policy, idempotency, audit, and exact browser or desktop leases
    - Coordinate the executor at explicit action boundaries with no hidden automation under operator control and reconcile screen, application, task, and authority state before resuming
    - Implement protected secret entry with screenshot, clipboard, event, log, and model-observation suppression and an explicit safe exit
    - Reconcile stale, lost, expired, duplicated, and restart-surviving leases without duplicate pointer, keyboard, browser, terminal, or clipboard effects
    - Expose Ion has control, You have control, and Control reconciling with precise expiry, needs-help, and unavailable consequences
    - Run real takeover, renewal, expiry, secret-entry, reconnect, control-plane restart, computer-host restart, and duplicate-safe resume journeys
    - _Requirements: 63.2-63.8, 65.2, 65.4-65.5_
  - [ ] 16.5 Railway one-click packaging, onboarding, health, and rollback
    - Build version-pinned Ion and ion-computer images plus a multi-service Railway template with only Ion public and no privileged runtime requirement
    - Configure authenticated dual-stack private networking, generated or sealed secrets, write-only provider references, explicit ports, public origin policy, and optional HNSW
    - Add Ion data and optional Personal workspace volumes with non-root ownership, start-time migrations, capacity checks, backups, export, restore, and redeploy-downtime guidance
    - Add liveness, readiness, compatibility, migration, continuous monitoring, restart, rollback, single-writer, serverless-sleep, and unavailable reconciliation
    - Add Railway-compatible bounded WebSocket reconnect and replay plus declared CPU, memory, storage, egress, idle, and cost projections before persistent compute
    - Test a fresh template locally and, only with explicit authorization, on Railway through ready or specific unavailable onboarding, rollback, restart, export, and non-lock-in migration
    - _Requirements: 64.1-64.8, 65.1-65.2, 65.7-65.8_
  - [ ] 16.6 Private Computer security, cleanup, performance, and production acceptance
    - Enforce least privilege, authenticated and rate-limited APIs, bounded egress, hostile-content containment, secret redaction, scoped volumes, and fail-closed platform capability checks
    - Run adversarial page, download, clipboard, terminal, cross-actor, cross-mode, stale-ticket, replay, lost-lease, reconnect-storm, orphan, exhaustion, storage, cleanup, version, and restart suites
    - Run the complete production journey from SearXNG-first research through graphical fallback, live desktop, needs-help takeover, duplicate-safe resume, verified artifact, restart, Personal retention, and Clean destruction
    - Publish image provenance and reference hardware, Railway plan, readiness, frame, input, reconnect, replay, CPU, memory, storage, egress, long-run, cleanup, and failure measurements
    - Prove first-contact preferred-name retention and the same Ion identity, memory, relationship, policy, approval, task, event, artifact, and evidence continuity across the complete journey
    - Reconcile Phase 16 status only after current production binaries, clients, container runtime, and authorized deployment evidence satisfy every criterion with unresolved platform boundaries visible
    - _Requirements: 62.7, 63.8, 65.1-65.8_
  - [ ] 16.7 Deployment username and password authentication
    - Add validated deployment username plus plaintext-password or Argon2id-hash configuration with startup derivation, zeroization, no credential persistence, and fail-closed Railway and non-loopback behavior
    - Implement exact-origin bounded login, generic constant-time verification, bounded throttling, authenticated status, logout, short-lived signed session cookies, CSRF binding, and credential invalidation
    - Gate the mounted SPA and every operator API behind authentication while preserving loopback-only local development and exposing no actor bootstrap or session credential before login
    - Build an accessible restrained login and logout experience and document secure Railway, Compose, container, and systemd variable configuration without hardcoded credentials
    - Verify static contracts and production builds, then run authentication, browser, security, restart, and deployment acceptance only when test and restart authorization is available
    - _Requirements: 67.1-67.7_

## Phase 17 — Image and Video Generation Studio

- [ ] 17. Secure Provider-Backed Media Creation, Editing, Durable Jobs, and Local Assets
  - [ ] 17.1 Mounted Novita image and video generation studio
    - Implement a replaceable Novita V3 adapter for text and image generation, text and image video generation, inpainting, cleanup, background tools, text removal, face merge, and upscaling
    - Persist actor-scoped encrypted requests, provider task correlation, progress, safe failures, restart reconciliation, and bounded locally owned output assets
    - Expose authenticated same-origin status, list, create, inspect, asset, download, and delete APIs with CSRF, origin, payload, media, and SSRF enforcement
    - Build and mount a responsive accessible Media Studio with task-specific controls, uploads and masks, previews, job progress, generation history, playback, downloads, retry, and unconfigured guidance
    - Generate contracts and production assets, then verify static, security, restart, provider, browser, accessibility, and end-to-end behavior without false completion
    - _Requirements: 66.1-66.7_

## Phase 18 — Office Studio

- [ ] 18. Encrypted Agent-Native Office Studio with ONLYOFFICE Editing
  - [ ] 18.1 Office Studio: models, engine contract, storage, API, proxy, UI, deployment, and test source
    - Implement Office domain models, encrypted SQLite store, version service, and bundled OOXML templates
    - Implement OfficeEngine contract, ONLYOFFICE adapter, signed file URLs, callback validation, and reconciliation
    - Implement authenticated Office HTTP handlers, same-origin reverse proxy, and control-plane operations
    - Add agent tools with bounded inspection, proposal types, and verified artifact registration
    - Build Office Studio route with library, real editor host, and honest failure states
    - Add Compose, Railway, Kubernetes, and Helm packaging with pinned images and private networking
    - Add documentation, generated docs, and complete test source without executing tests
    - Verify static builds and typechecks only; leave task in_progress because live tests are prohibited
    - _Requirements: 68.1-68.12_

## Task Dependency Graph

```json
{
  "waves": [
    { "id": 1,  "tasks": ["1.1", "1.2"] },
    { "id": 2,  "tasks": ["1.3", "1.4"] },
    { "id": 3,  "tasks": ["1.5"] },
    { "id": 4,  "tasks": ["2.1"] },
    { "id": 5,  "tasks": ["2.2", "2.3"] },
    { "id": 6,  "tasks": ["2.4"] },
    { "id": 7,  "tasks": ["3.1", "3.2"] },
    { "id": 8,  "tasks": ["3.3"] },
    { "id": 9,  "tasks": ["4.1", "4.2"] },
    { "id": 10, "tasks": ["5.1", "5.2"] },
    { "id": 11, "tasks": ["6.1", "6.2"] },
    { "id": 12, "tasks": ["6.3", "6.4"] },
    { "id": 13, "tasks": ["7.1"] },
    { "id": 14, "tasks": ["7.2", "8.1"] },
    { "id": 15, "tasks": ["9.1"] },
    { "id": 16, "tasks": ["9.2"] },
    { "id": 17, "tasks": ["9.3"] },
    { "id": 18, "tasks": ["9.4"] },
    { "id": 19, "tasks": ["9.5"] },
    { "id": 20, "tasks": ["10.1"] },
    { "id": 21, "tasks": ["10.2"] },
    { "id": 22, "tasks": ["10.3"] },
    { "id": 23, "tasks": ["11.1"] },
    { "id": 24, "tasks": ["11.2"] },
    { "id": 25, "tasks": ["11.3", "11.4"] },
    { "id": 26, "tasks": ["11.5"] },
    { "id": 27, "tasks": ["12.1"] },
    { "id": 28, "tasks": ["13.1"] },
    { "id": 29, "tasks": ["13.2", "13.3"] },
    { "id": 30, "tasks": ["13.4"] },
    { "id": 31, "tasks": ["13.5"] },
    { "id": 32, "tasks": ["13.6"] },
    { "id": 33, "tasks": ["13.7"] },
    { "id": 34, "tasks": ["13.8", "13.9"] },
    { "id": 35, "tasks": ["13.10", "13.14", "16.7", "17.1", "18.1"] },
    { "id": 36, "tasks": ["13.11", "13.12"] },
    { "id": 37, "tasks": ["13.13"] },
    { "id": 38, "tasks": ["14.1"] },
    { "id": 39, "tasks": ["14.2", "14.3"] },
    { "id": 40, "tasks": ["14.4"] },
    { "id": 41, "tasks": ["14.5", "14.6"] },
    { "id": 42, "tasks": ["14.7", "14.8", "14.9"] },
    { "id": 43, "tasks": ["15.1"] },
    { "id": 44, "tasks": ["16.1"] },
    { "id": 45, "tasks": ["16.2"] },
    { "id": 46, "tasks": ["16.3"] },
    { "id": 47, "tasks": ["16.4", "16.5"] },
    { "id": 48, "tasks": ["16.6"] }
  ]
}
```
